var username = 'test010@tenko22.cher-ish.net'; // enter a valid account herevar password = 'yashinomi2013'; // enter a valid password herevar pop3 = require('waf-mail/POP3'); var toread = new pop3.POP3(); // create a new POP3 object    // We need to connect to the POP3 servertoread.connect('mail.tenko22.cher-ish.net', 110, false, function (isOk, response) {     if (!isOk) {        close();        return;    }        // Authentication is required first       toread.authenticate(username, password, function (isOk, response) {        if (!isOk) {                        close();            return;                  }            // We want the last available email                toread.getStatus(function(isOk, response, numberMessages, totalSize) {            if (!isOk) {                                close();                return;                            }                                console.log(numberMessages + ' messages, total size of ' + totalSize + ' octets.');                toread.retrieveMessage(numberMessages, function (isOk, mailObj) {                if (isOk) {                        var i;                            for (i = 0; i < mailObj[0].length; i++)    {                                        console.log(mailObj[0][i]);                            //console.log(mailObj[i][1]);                              //console.Log(mailObj[i].subject +  '\n');                               // mailObj[i].getHeader();                                            }                                                  }                 close();                            });               });    });    });wait(10000);toread.quit();